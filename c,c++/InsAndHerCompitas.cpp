#include <bits/stdc++.h>
using namespace std;
#define int long long

void fastIO() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cerr.tie(0);
}

int main() {
    fastIO();

    // int n, m, op, id1, id2, x, y, i;
    // vector<pair<int,int>> puntajes(4);
    // cin >> n >> m;

    // for(int i = 0; i < m; i++) {
    //     id1=0; id2=0;
    //     cin >> x >> y;
    //     for (int k = 0; k < n; k++) {
    //         cin >> op;
    //         puntajes[k].first = op;
    //         if (op == 1) {
    //             id1++;
    //         } else {
    //             id2++;
    //         }
    //     }
    //     if (floor((float)x/(float)(id1+1)) > y) {
    //         for(int k = 0; k < n; k++) {
    //             puntajes[k].second += floor((float)x/(float)(id1+1));
    //             i += floor((float)x/(float)(id1+1));
    //         }
    //     }
    // }

    return 0;
}